import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['example.com'], // Add all remote domains you use for images
  },
};

export default nextConfig;
